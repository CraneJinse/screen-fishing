# 开发与贡献

使用 Electron + 原生 HTML/CSS/JS。入口 main.js，预加载 preload.js；当前规则以 src/game-state-runtime.js 为准，src/game-state.js 提供基础鱼表。概率、成就、存档分别在对应模块。

`npm ci` 后先运行 `npm run check` 与 `npm test`。规则变更补充边界测试；窗口变更检查显示/隐藏/托盘/快捷键。`npm run build` 生成独立 Windows 便携输出。所有测试使用临时数据，不提交 user-data、artifacts 或 dist。

此仓库包含当前钓鱼版运行源码、素材与相关测试；已封存的鱼缸界面、早期原型和美术生产中间文件不在此目录。少量鱼缸状态模块/目录数据仅用于旧存档兼容。

提交问题时说明版本、系统、预期行为、实际结果、复现步骤。代码遵循 MIT；添加或替换素材前须记录来源与许可，不能仅因图片能下载就将其作为自由素材提交。

公开测试保留批准美术的SHA-256基线；美术生产源和旧原型生成器未打包，所以不在此仓库执行旧生成器的字节稳定性测试。运行素材完整性与图层哈希仍会检查。
